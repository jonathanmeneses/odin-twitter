<div class="d-flex flex-start mt-4">
  <%# Placeholder images %>
  <%= link_to user_path(comment.user), class: "me-3", data: { turbo_frame: "_top" }  do %>
    <% if comment.user.avatar.attached? %>
      <%= image_tag comment.user.avatar, class:"rounded-circle shadow-1-strong avatar-dims" %>
    <%else%>
      <%= image_tag 'deer.png', class:"rounded-circle shadow-1-strong avatar-dims"%>
    <% end %>
  <% end %>
  <div class="flex-grow-1 flex-shrink-1">
    <div>
      <div class="d-flex justify-content-between align-items-center">
        <p class="mb-1">
          <%=comment.user.username%> <span class="small">- <%=time_display(comment.created_at)%></span>
        </p>
      </div>
      <p class="small mb-0">
        <%= comment.content%>
      </p>
    </div>
  </div>
</div>